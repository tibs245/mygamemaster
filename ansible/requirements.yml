---
# Collections recommandées. Installer avec :
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: containers.podman
    version: ">=1.13.0"
  - name: ansible.posix
    version: ">=1.5.0"
